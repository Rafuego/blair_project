/**
 * Temporary stand-in for the About sections whose assets are still being
 * pulled from Figma: investors + quote, founder profiles, team grid, provider
 * directors, both advisory boards, the press logo bar and the audience cards.
 * Each lands as its own component and replaces this.
 */
export function AboutBandsPlaceholder() {
  return null;
}
